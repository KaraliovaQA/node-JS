// Базовые концепции и установка (Basic Concepts & Setup Environment)


const userName = 'Sweta';

const sayHi = (userName) => `Hello, my name is ${userName}`;

// console.log(sayHi(userName));

// module.exports = userName;

module.exports = {
   userName,
   sayHi,
};